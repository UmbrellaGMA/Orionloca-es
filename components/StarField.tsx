import React from 'react';

// StarField mantido para compatibilidade mas renderiza vazio.
// O background visual agora é feito via CSS no Hero (grid lines + radial gradient).
const StarField: React.FC = () => null;

export default StarField;